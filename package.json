{
  "name": "nutrisyncbackend",
  "version": "1.0.0",
  "description": "Backend server that will handle all data transactions from the Nutrisync App including user auth, food data, direct data fetching from the USDA API, and storing user data. ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mysql": "^2.18.1"
  }
}
